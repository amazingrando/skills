# Productivity

General workflow tools that aren't code-specific — planning, writing, communication.

Add each skill as a folder here containing a `SKILL.md`, then register its path
in `../../.claude-plugin/plugin.json`.
